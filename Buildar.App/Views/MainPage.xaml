<Page
    x:Class="Buildar.App.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Style="{StaticResource PageStyle}"
    mc:Ignorable="d">

    <Grid x:Name="ContentArea" Margin="{StaticResource MediumLeftRightMargin}">
        <Grid>
            <GridView IsItemClickEnabled="True" >
                <GridView.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="5">
                            <Border Background="LightGray" Height="200" Width="200">
                                <TextBlock Text="{Binding}" 
                               FontSize="30" Foreground="Black" TextWrapping="Wrap" TextAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                    </DataTemplate>
                </GridView.ItemTemplate>
                <GridView.Items>
                    <x:String>Build your own computer</x:String >
                    <x:String>Community build</x:String>
                    <x:String>High End</x:String>
                    <x:String>Mid Range</x:String>
                    <x:String>Budget Build</x:String>
                </GridView.Items>
            </GridView>
        </Grid>
    </Grid>
</Page>
